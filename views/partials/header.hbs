


<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><img src="/images/y.png" alt="Y" width="24" height="24"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
        
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        {{#unless session.loggedIn}}
        <button type="submit" class="form-control" id="signupButton">Sign Up</button>
        {{/unless}}
        {{#if session.loggedIn}}
        <span class="navbar-text">Logged in as {{session.author_name}}</span>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/dashboard">Dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="user/logout">Logout</a>
        </li>
        {{/if}}
      </ul>
    </div>
        {{#unless session.loggedIn}}
        <form class="d-flex" action="/user/login" method="post">
        <div class="input-group">
            <input type="text" name="email" id="userFormEmail" class="form-control" placeholder="email" />
            <input type="password" name="password" id="userFormPassword" class="form-control" placeholder="Password" />
            <input type="submit" class="form-control" value="Login" />
        </div>
        {{/unless}}
  </div>
</nav>


{{!-- <div class="header">
    <img src="/images/y.png" alt="Y logo" class="logo">
    <h1 class="title">Y</h1>
    
    {{#if session.loggedIn}}
        <p>Logged in as {{session.author_name}}</p>
        <a href="user/logout">Logout</a>
    {{else}}
            
            <button type="submit" id="signupButton">Sign Up</button>
    {{/if}}

</div>

<form class="userForm" action="/user/login" method="post">
    <input type="text" name="email" id="userFormEmail" placeholder="email" />
    <input type="password" name="password" id="userFormPassword" placeholder="Password" />
    <input type="submit" value="Login" />
</form> --}}
